# Utilities for STM8 firmware development

- `fuse-calc` : Fuse bit calculator (using OpenOffice Calc).
- `timer` : ARR registry value calculator for STM8 series MCUs.
- `uart` : Baud rate calculator.
